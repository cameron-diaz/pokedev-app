import { createSlice } from "@reduxjs/toolkit";
import { AppTypeIntialState } from "../../utils/Types";

const initialState:AppTypeIntialState = {}

export const AppSlice = createSlice({
    name: "app",
    initialState,
    reducers: {},
    
})